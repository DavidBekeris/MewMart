@using MewMart.Models
@model List<Product>

@{
    ViewData["Title"] = "Produkter - MewMart";
}


<div class="card shadow border my-4">
    <dív class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">
                    Produkt lista
                </h2>
            </div>
        </div>
    </dív>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Skapa ny produkt
                </a>
            </div>
        </div>

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Produkt Namn
                    </th>
                    <th>
                        ISBN
                    </th>
                    <th>
                        Pris
                    </th>
                    <th>
                        Tillverkare
                    </th>
                    <th>
                        Kategori
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.Title</td>
                        <td>@obj.ISBN</td>
                        <td>@obj.ListPrize</td>
                        <td>@obj.Manufacturer</td>
                        <td>@obj.CategoryId</td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controler="Product" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Redigera
                                </a>
                                <a asp-controler="Product" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3"></i> Radera
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
